import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';

const serverSource = readFileSync(new URL('../server.js', import.meta.url), 'utf8');
const detectTopicSource = readFileSync(new URL('../src/logic/kimon/detectTopic.js', import.meta.url), 'utf8');
const promptBuilderSource = readFileSync(new URL('../src/logic/kimon/promptBuilder.js', import.meta.url), 'utf8');
const strategyPromptSource = readFileSync(new URL('../src/logic/kimon/strategyPrompt.js', import.meta.url), 'utf8');
const modelRouterSource = readFileSync(new URL('../src/logic/kimon/modelRouter.js', import.meta.url), 'utf8');

assert.match(serverSource, /import \{ canonicalizeTopicKey, detectDeepDive, detectTopicHybrid \} from '\.\/src\/logic\/kimon\/detectTopic\.js';/);
assert.match(serverSource, /import \{ detectQuestionIntent \} from '\.\/src\/logic\/kimon\/questionIntent\.js';/);
assert.doesNotMatch(serverSource, /import \{ detectFlagCombos \} from '\.\/src\/logic\/dungThan\/flagCombos\.js';/);
assert.match(serverSource, /const isDeepDive = !isAutoLoad && detectDeepDive\(userContext\);/);
assert.match(serverSource, /function getCriticalTopicFlagCombos\(selectedTopicFlags = \[\]\)/);
assert.match(serverSource, /const FLASH_LOCK_TOPICS = new Set\(\['hoc-tap', 'thi-cu'\]\);/);
assert.match(serverSource, /function topicAllowsCriticalFlagEscalation\(topic = ''\) \{\s*return !FLASH_LOCK_TOPICS\.has\(topic\);\s*\}/s);
assert.match(serverSource, /const VERDICT_STRATEGY_AXES = new Set\(\['yes_no', 'timing', 'pricing', 'state', 'person'\]\);/);
assert.match(serverSource, /function shouldForceVerdictStrategy\(\{ topic = '', userContext = '' \} = \{\}\) \{\s*if \(!topic \|\| topic === 'chung'\) return false;\s*const intent = detectQuestionIntent\(userContext\);\s*return VERDICT_STRATEGY_AXES\.has\(intent\?\.key \|\| ''\);\s*\}/s);
assert.match(serverSource, /function normalizeHintTopicKey\(topicKey = ''\) \{\s*return canonicalizeTopicKey\(String\(topicKey \|\| ''\)\.trim\(\)\.toLowerCase\(\)\) \|\| 'chung';\s*\}/s);
assert.match(serverSource, /function getGeminiApiKey\(\)/);
assert.match(serverSource, /function getSafeKimonErrorMessage\(error\)/);
assert.match(serverSource, /function shouldRetryStrategyAsTopic\(error, \{ effectiveTier = '', topic = '' \} = \{\}\)/);
assert.match(serverSource, /strategy failed for topic=\$\{topic\}; retrying with topic tier:/);
assert.match(serverSource, /const canRetryAsTopic = shouldRetryStrategyAsTopic\(primaryError, \{ effectiveTier, topic \}\)\s*&& streamedChunkCount === 0;/);
assert.match(serverSource, /const criticalTopicFlagCombos = getCriticalTopicFlagCombos\(enrichedQmdjData\?\.selectedTopicFlags\);/);
assert.match(serverSource, /const allowCriticalFlagEscalation = topicAllowsCriticalFlagEscalation\(topic\);/);
assert.match(serverSource, /const hasCriticalTopicFlags = allowCriticalFlagEscalation && criticalTopicFlagCombos\.length > 0;/);
assert.match(serverSource, /flags: \['Dịch Mã', 'Phục Ngâm'\]/);
assert.match(serverSource, /flags: \['Dịch Mã', 'Phản Ngâm'\]/);
assert.match(serverSource, /flags: \['Không Vong', 'Phục Ngâm'\]/);
assert.match(serverSource, /flags: \['Không Vong', 'Phản Ngâm'\]/);
assert.doesNotMatch(serverSource, /HORSE_VOID/);
assert.match(serverSource, /const forceStrategyTopic = \['tai-van', 'muu-luoc', 'chien-luoc'\]\.includes\(topic\);/);
assert.match(serverSource, /const forceVerdictStrategy = shouldForceVerdictStrategy\(\{ topic, userContext \}\);/);
assert.match(serverSource, /const effectiveTier = \(isDeepDive \|\| hasCriticalTopicFlags \|\| forceStrategyTopic \|\| forceVerdictStrategy\) \? 'strategy' : tier;/);
assert.match(serverSource, /verdictStrategy=\$\{forceVerdictStrategy\}/);
assert.match(serverSource, /function getKimonResponseModeLabel\(data = \{\}\)/);
assert.match(serverSource, /if \(tier === 'strategy'\) return 'thinking';/);
assert.match(serverSource, /if \(tier === 'topic' \|\| tier === 'companion'\) return 'flash';/);
assert.match(serverSource, /modeLabel: getKimonResponseModeLabel\(data\)/);
assert.match(serverSource, /className = 'kymon-footer-mode'/);
assert.match(serverSource, /className = 'kymon-footer-text'/);
assert.match(serverSource, /if \(!apiKey\) \{\s*endSseWithFallback\(res, \{\s*tier: effectiveTier,\s*topic: topic \|\| 'chung',\s*message: GEMINI_API_CONFIG_MESSAGE,/s);
assert.match(serverSource, /if \(!apiKey\) \{\s*respondWithKimonFallback\(res, \{\s*statusCode: 200,\s*tier: effectiveTier,\s*topic: topic \|\| 'chung',\s*message: GEMINI_API_CONFIG_MESSAGE,/s);
assert.match(serverSource, /respondWithKimonFallback\(res, \{\s*statusCode: 200,\s*tier: resolvedTier,\s*topic: resolvedTopic,\s*message: getSafeKimonErrorMessage\(error\),/s);
assert.match(serverSource, /endSseWithFallback\(res, \{\s*tier: resolvedTier,\s*topic: resolvedTopic,\s*message: getSafeKimonErrorMessage\(error\),/s);
assert.doesNotMatch(serverSource, /message:\s*isTimeoutError\(error\)\s*\?\s*KIMON_SERVICE_UNAVAILABLE_MESSAGE\s*:\s*\(error\??\.?message \|\| KIMON_SERVICE_UNAVAILABLE_MESSAGE\)/);
assert.doesNotMatch(serverSource, /selectedTopicFlags\.length > 0/);
assert.doesNotMatch(serverSource, /\bcurrentTopic\b/);
assert.doesNotMatch(serverSource, /function renderSuggestions\(/);
assert.doesNotMatch(serverSource, /function appendGenericKimonSection\(/);
assert.doesNotMatch(serverSource, /function appendNarrativeSection\(/);

assert.match(detectTopicSource, /You are an intent classifier\./);
assert.match(detectTopicSource, /return a strict JSON object only/i);
assert.match(detectTopicSource, /Return exactly this shape:/);
assert.match(detectTopicSource, /const STRATEGY_ONLY_TOPICS = new Set\(\['muu-luoc', 'chien-luoc'\]\);/);
assert.match(detectTopicSource, /const hasDeepDiveIntent = detectDeepDive\(userMessage\);/);
assert.match(detectTopicSource, /function collectKeywordTopicStats\(/);
assert.match(detectTopicSource, /function applyPrimaryOverlapOverride\(/);
assert.match(detectTopicSource, /const overlapOverride = applyPrimaryOverlapOverride\(rankedDomainStats, msgNorm\);/);
assert.match(detectTopicSource, /secondaryTopic: secondaryTopic \? canonicalizeTopicKey\(secondaryTopic\) : null,/);
assert.match(detectTopicSource, /topicCandidates,/);
assert.match(detectTopicSource, /tier: hasDeepDiveIntent \? 'strategy' : getTier\(canonicalTopic\)/);
assert.match(detectTopicSource, /const bestStrategyTopic = rankedStrategyStats\[0\]\?\.topic \|\| findBestKeywordTopic\(/);
assert.doesNotMatch(detectTopicSource, /Kymon - Một Chiến lược gia AI/);

assert.match(strategyPromptSource, /export const KYMON_PRO_SYSTEM_PROMPT = /);
assert.match(strategyPromptSource, /\[SYSTEM ROLE & PERSONA\]/);
assert.match(strategyPromptSource, /\[CORE METAPHORS - TỪ ĐIỂN ẨN DỤ BẮT BUỘC\]/);
assert.match(strategyPromptSource, /\[STRICT CONSTRAINTS - RÀNG BUỘC NGHIÊM NGẶT\]/);
assert.match(strategyPromptSource, /\[DEEP DIVE & CHAIN OF THOUGHT - CHUỖI TƯ DUY SÂU SẮC\]/);
assert.match(strategyPromptSource, /\[SHADOW ANALYSIS - GÓC KHUẤT BẮT BUỘC\]/);
assert.match(strategyPromptSource, /dễ tự huyễn hoặc/i);
assert.match(strategyPromptSource, /cái giá phải trả/i);
assert.match(strategyPromptSource, /follow-up question thật trúng/i);
assert.match(strategyPromptSource, /\[OUTPUT FORMAT - STRATEGY JSON BẮT BUỘC\]/);
assert.match(strategyPromptSource, /markdown bold/i);
assert.match(strategyPromptSource, /KHÔNG VUỐT VE/i);
assert.match(strategyPromptSource, /JSON object với chính xác 5 key sau/i);
assert.match(strategyPromptSource, /buildStrategySystemInstruction\(\{ groundingBundle = null \} = \{\}\) \{\s*return appendGroundingSystemRules\(KYMON_PRO_SYSTEM_PROMPT, groundingBundle\);/s);

assert.match(promptBuilderSource, /export const KYMON_TOPIC_SYSTEM_PROMPT = /);
assert.match(promptBuilderSource, /buildKimonSystemInstruction\(\{ tier = 'topic', groundingBundle = null \} = \{\}\) \{\s*void tier;\s*return appendGroundingSystemRules\(KYMON_TOPIC_SYSTEM_PROMPT, groundingBundle\);/s);
assert.match(promptBuilderSource, /\[INTERNAL INSIGHTS\]/);
assert.match(promptBuilderSource, /\[PHÂN TÍCH CHỦ ĐỀ:/);
assert.match(promptBuilderSource, /\[FLAGS ENGINE TẠI DỤNG THẦN/);
assert.match(promptBuilderSource, /\[ĐIỂM CẦN BÁM\]/);
assert.match(promptBuilderSource, /\[SYSTEM ROLE & PERSONA\]/);
assert.match(promptBuilderSource, /\[OUTPUT FORMAT - TOPIC JSON\]/);
assert.match(promptBuilderSource, /3 key: "lead", "message", "closingLine"/);
assert.doesNotMatch(promptBuilderSource, /\[CORE METAPHORS - TỪ ĐIỂN ẨN DỤ BẮT BUỘC\]/);
assert.doesNotMatch(promptBuilderSource, /\[OUTPUT FORMAT - QUY TRÌNH 4 BƯỚC BẮT BUỘC\]/);

assert.match(modelRouterSource, /import \{ buildKimonPrompt, buildCompanionPrompt, buildKimonSystemInstruction \} from '\.\/promptBuilder\.js';/);
assert.match(modelRouterSource, /import \{ buildStrategyPrompt, buildStrategySystemInstruction \} from '\.\/strategyPrompt\.js';/);
assert.match(modelRouterSource, /systemPrompt: buildKimonSystemInstruction\(\{ groundingBundle \}\),/);

console.log('kimon-deep-dive-routing.mjs: OK');
